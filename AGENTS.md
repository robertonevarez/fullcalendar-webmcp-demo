# Blume Documentation Site Rules

- Use `blume dev` for local development.
- Documentation pages are written in MDX under `docs/`.
- Navigation and folder hierarchy are managed via `meta.ts` in each directory and `blume.config.ts`.
- The OpenAPI specification is located at `public/openapi.json`.
